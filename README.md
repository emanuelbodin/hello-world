# hello-world

Simple [Axum](https://github.com/tokio-rs/axum) server that returns `Hello, World!` on `GET /`.

## Run locally

```bash
cargo run
```

Then open http://localhost:3000. Set `PORT` to override the port (Railway sets this automatically).
